using System;

namespace MathLibraryProject {
    class Program {
        static void Main(string[] args) {
            var a = Math.Add("1", "2"); //var has to be assigned however can be used in place of specific types
        }
    }
}
